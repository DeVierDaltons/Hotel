<UserControl x:Class="Hotel.View.GuestsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="700" d:DataContext="{d:DesignData /SampleData/GuestsViewModelSampleData.xaml}"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1">
            <Button Content="Add Guest" Click="OnAddGuestClicked"/>
            <Button Content="Edit Selected Guest" Click="OnEditGuestClicked"/>
        </StackPanel>
        <DataGrid ItemsSource="{Binding Guests, Mode=TwoWay}" Name="GuestsList" Grid.Column="0" AutoGenerateColumns="False" IsReadOnly="True" >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding FullName}" Header="Name"/>
                <DataGridTextColumn Binding="{Binding EmailAdress}" Header="Email"/>
                <DataGridTextColumn Binding="{Binding PhoneNumber}" Header="Phone"/>
                <DataGridTextColumn Binding="{Binding Country}" Header="Country"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
