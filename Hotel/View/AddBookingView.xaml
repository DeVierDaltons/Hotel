<Window x:Class="Hotel.View.AddBookingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel.ViewModel"
        mc:Ignorable="d"
        Title="AddBookingView" Height="180" Width="300"
        >
    <Window.DataContext>
        <local:AddBookingViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Guest:"/>
        <ComboBox Grid.Row="0" Grid.Column="1" Name="GuestDropdown" SelectedItem="{Binding Guest}"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Room:"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Name="RoomDropdown" SelectedItem="{Binding Room}"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Begin date:"/>
        <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding StartDay}"/>

        <Label Grid.Row="3" Grid.Column="0" Content="End date:"/>
        <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding EndDay}"/>

        <Button Grid.Row ="5" Grid.Column="0" Grid.ColumnSpan="2" Command="{Binding AddBookingCommand}" Content="Save"></Button>
    </Grid>
</Window>
